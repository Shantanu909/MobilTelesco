# Ignore everything in the repository
*

# Don't ignore the .gitignore file itself
!.gitignore

# Don't ignore the .gitkeep file (to preserve folder structure)
!.gitkeep

# Ignore all folders (and their contents) except .gitkeep
01-Dec-2024err/*
01-Dec-2024err/!.gitkeep